import math
import random


def evaluate_board(board, color):
    """
    Evaluates the board state for the AI.
    Returns a positive number if 'color' is winning, negative if losing.
    """
    # TODO: هنا تضع منطق تقييم اللعبة (عدد القطع، الأماكن الاستراتيجية، إلخ)
    # هذا مجرد رقم عشوائي مؤقت
    return random.randint(-10, 10) 

